<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFFFFF" /> <!-- Fondo blanco -->
    <stroke
        android:width="3dp"
        android:color="@color/blue" /> <!-- Borde naranja -->
    <corners android:radius="8dp" /> <!-- Bordes redondeados -->
    <padding
        android:left="8dp"
        android:top="4dp"
        android:right="8dp"
        android:bottom="4dp" /> <!-- Espaciado interno -->
</shape>
